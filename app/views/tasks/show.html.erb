<h1><%=@task.title %></h1>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<%= link_to "Back to the list", tasks_path %>
<%= link_to "Edit this task", edit_path %>
<%= link_to "Delete this task", delete_path(@task), method: :delete, data: {confirm: "Are you sure?"} %>
